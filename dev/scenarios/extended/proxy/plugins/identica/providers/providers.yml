conflicts:
  username:
    defaultRule:
      providers: []
      force: false
      resolvers:
        - id: "format_display"
          parameters:
            format:
              pattern: "{username}*"
            target: "joiner"
    pairs:
      - providers:
          - "premium"
          - "password"
        force: false
        resolvers:
          - id: "format_display"
            parameters:
              format:
                pattern: "{username}_{incomingProvider}"
              target: "joiner"
providers:
  - id: "password"
    displayName: "CR"
    enabled: true
    priority: 50
    overrides: {}
    verification:
      enabled: true
      required: false
      methods:
        - id: "totp"
          enabled: true
          priority: 100
          unavailableSelectionPolicy: "KEEP_LOCKED"
    entrypoints:
      - "password.arcadeya.com"
  - id: "premium"
    displayName: "PR"
    enabled: true
    priority: 100
    overrides:
      sessionTtl: "12h"
    verification:
      enabled: false
      required: false
      methods: []
    entrypoints:
      - "premium.arcadeya.com"
