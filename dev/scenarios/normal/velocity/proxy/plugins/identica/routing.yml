defaults:
  step:
    target: "lobby"
    attempts:
      mode: "UNTIL_REACHED"
      retryDelay: "1s"
  complete:
    target: "lobby"
    attempts:
      mode: "UNTIL_REACHED"
      retryDelay: "1s"
      consumeOnReached: true
scenarios: {}
