defaults:
  step:
    target: "auth"
  complete:
    target: "lobby"
    attempts:
      mode: "UNTIL_REACHED"
      retryDelay: "1s"
      consumeOnReached: true
scenarios:
  registration:
    step:
      target: "registration"
  migration:
    step:
      target: "migration"
