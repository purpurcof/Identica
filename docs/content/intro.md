---
sidebar_position: 1
slug: /
---

# Identica

Identica documentation lives here and is published as a static Docusaurus site on each configured release flow.

## Project Docs

Add Markdown or MDX files under this `docs` directory. The documentation pipeline builds this directory into a static site and publishes a container image for Kubernetes.
