---
title: Restriction
description: Shared configuration and extension points for the generic restriction capability.
---

The `restriction` capability provides the generic framework for provider restriction types.
It does not define the behavior of every restriction itself.

Shared restriction settings live under `providers/capabilities/restriction/settings`.

Provider-specific restriction overrides live under `providers[].capabilities.restriction...`.