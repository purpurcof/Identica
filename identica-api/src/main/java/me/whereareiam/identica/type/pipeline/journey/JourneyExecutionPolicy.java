package me.whereareiam.identica.type.pipeline.journey;

/**
 * Defines how execution should proceed across blocks in an execution plan.
 */
public enum JourneyExecutionPolicy {
	SEQUENTIAL,
	FALLBACK
}
